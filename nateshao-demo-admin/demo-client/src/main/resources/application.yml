server:
  port: 8084

spring:
  application:
    name: admin-client
  boot:
    admin:
      client:
        url: http://localhost:8083

management:
  endpoints:
    web:
      exposure:
        include: "*" 